import { AvatarCard } from '../../types/card';

// This file previously contained example cards for demonstrating conditional damage mechanics.
// All example cards have been removed as they were placeholder implementations without actual artwork.

export const conditionalDamageCards: AvatarCard[] = [];

// Utility function to get conditional damage cards by type
export const getConditionalCardsByType = (conditionType: string): AvatarCard[] => {
  return [];
};
